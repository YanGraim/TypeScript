"use strict";
let estaLogado;
let codeStatus;
// Tudo que for diferente de 0, string vazia e undefined será verdadeiro
codeStatus = 1;
// convertendo variavel para o tipo boolean
estaLogado = Boolean(codeStatus);
console.log(estaLogado);
